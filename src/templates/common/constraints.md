**Constraints:**
- NEVER hardcode API keys or secrets in source code or config files. Always use environment variable references.
- Follow the project's existing code style and conventions.
- Use OpenTelemetry semantic conventions for attribute names.
- Prefer the framework's idiomatic patterns for middleware, configuration, and dependency injection.
